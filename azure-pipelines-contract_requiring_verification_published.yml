# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

# trigger:
# - master

# parameters:
# - name: PACT_WEBHOOK_URL
#   type: string
# - name: PACT_WEBHOOK_GIT_COMMIT
#   type: string
# - name: PACT_WEBHOOK_GIT_BRANCH
#   type: string
# - name: PACT_WEBHOOK_DESCRIPTION
#   type: string

pool:
  vmImage: ubuntu-latest

steps:
# - checkout: self@PACT_WEBHOOK_GIT_COMMIT

- script: make ci_webhook
  displayName: ✅  Run tests
  env:
    PACT_URL: https://testdemo.pactflow.io/pacts/provider/pactflow-example-provider-dotnet/consumer/pactflow-example-consumer-dotnet/version/0341c650fd4e0ca1b00f7c67222e95521e387075
    PACT_BROKER_BASE_URL: https://testdemo.pactflow.io
    PACT_BROKER_TOKEN: $(PACT_BROKER_TOKEN)
    PACT_BROKER_PUBLISH_VERIFICATION_RESULTS: true  
    GIT_COMMIT: $(Build.SourceVersion)
    GIT_BRANCH: $(Build.SourceBranchName)